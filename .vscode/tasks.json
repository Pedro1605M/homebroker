{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "javac --module-path lib --add-modules javafx.controls,javafx.fxml -cp \"lib/*\" -d bin src/*.java && copy /Y src\\*.fxml bin\\",
            "group": "build",
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            },
            "problemMatcher": "$javac"
        }
    ]
}
